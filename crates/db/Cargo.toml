[package]
name = "emberchamber-db"
description = "SQLx queries, connection pool helpers, and SQL migrations"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
sqlx = { workspace = true }
anyhow = { workspace = true }
