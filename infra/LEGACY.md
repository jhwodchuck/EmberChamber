# Legacy Path

`infra/docker-compose.yml` belongs to the older centralized prototype stack.

- Do not treat this directory as the active beta deployment model.
- Use [`../apps/relay`](../apps/relay), [`../apps/web`](../apps/web), and the current docs in [`../docs`](../docs) for active runtime work.
- Only touch this path for explicit legacy maintenance or migration reference.
