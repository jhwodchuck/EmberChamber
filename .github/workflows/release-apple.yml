name: Release - Apple Deferred

on:
  workflow_dispatch:

jobs:
  deferred:
    name: iPhone and macOS are phase 2
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "EmberChamber beta launch targets are Android, Windows, and Ubuntu."
          echo "Apple packaging is intentionally deferred until the Android and desktop beta is stable."
