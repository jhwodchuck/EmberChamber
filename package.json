{
  "name": "emberchamber",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/relay\" \"npm run dev --workspace=apps/web\"",
    "dev:legacy": "concurrently \"npm run dev --workspace=apps/api\" \"npm run dev --workspace=apps/web\"",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/protocol && npm run build --workspace=apps/relay && npm run build --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web",
    "build:relay": "npm run build --workspace=apps/relay",
    "dev:desktop": "npm run dev --workspace=apps/desktop",
    "doctor:desktop": "npm run doctor --workspace=apps/desktop",
    "build:desktop": "npm run build --workspace=apps/desktop",
    "relay:local": "bash scripts/start-local-relay.sh",
    "relay:local:screen": "bash scripts/start-local-relay.sh --screen",
    "relay:local:stop": "bash scripts/start-local-relay.sh --stop",
    "invite:local:test": "bash scripts/create-local-test-invite.sh",
    "install:desktop:ubuntu": "bash scripts/install-ubuntu-desktop.sh",
    "ubuntu:ready": "bash scripts/ubuntu-ready.sh",
    "dev:mobile": "npm run dev --workspace=apps/mobile",
    "build:android": "npm run build:android --workspace=apps/mobile",
    "build:ios": "npm run build:ios --workspace=apps/mobile",
    "test": "npm run test --workspace=apps/relay && cargo test -p emberchamber-core -p emberchamber-relay-protocol",
    "test:legacy": "npm run test --workspace=apps/api",
    "lint": "npm run lint --workspace=apps/web && npm run lint --workspace=apps/relay",
    "type-check": "npm run type-check --workspace=packages/protocol && npm run type-check --workspace=apps/relay && npm run type-check --workspace=apps/web && npm run build --workspace=packages/shared && cargo check -p emberchamber-core -p emberchamber-relay-protocol",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3",
    "turbo": "^2.3.3"
  }
}
