{
  "buildCommand": "npm run build --workspace=packages/shared && npm run build --workspace=packages/protocol && npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm ci --no-fund --loglevel=error",
  "framework": "nextjs"
}
