{
  "name": "@emberchamber/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "EmberChamber native desktop shell for Windows, Linux, and macOS packaging",
  "scripts": {
    "dev": "tauri dev",
    "doctor": "tauri info",
    "verify": "npm run doctor && npm run build",
    "build": "tauri build",
    "android:init": "tauri android init",
    "build:android": "tauri android build",
    "ios:init": "tauri ios init",
    "build:ios": "tauri ios build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.1"
  }
}
