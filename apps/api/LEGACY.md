# Legacy Path

`apps/api` is the retired Express and Postgres prototype.

- Do not route new beta backend work here by default.
- Use [`../relay`](../relay) for the current relay and control-plane runtime.
- Keep changes narrowly scoped to explicit legacy maintenance or verification needs.
