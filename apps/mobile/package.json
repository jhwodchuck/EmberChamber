{
  "name": "@emberchamber/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "App.tsx",
  "scripts": {
    "dev": "expo start --clear",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "prebuild": "expo prebuild --platform android --clean",
    "prebuild:ios": "expo prebuild --platform ios --clean",
    "build:android": "expo prebuild --platform android --non-interactive",
    "build:ios": "expo prebuild --platform ios --non-interactive",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "expo": "~55.0.0",
    "expo-image-picker": "~55.0.14",
    "expo-screen-capture": "~8.0.9",
    "expo-secure-store": "~55.0.9",
    "expo-sqlite": "~55.0.11",
    "react": "19.2.0",
    "react-native": "0.83.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "typescript": "^5.4.5"
  }
}
